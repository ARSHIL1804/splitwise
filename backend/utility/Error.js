const ErrorCodes = Object.freeze({
    BAD_REQUEST: 400,
    SERVER_ERROR: 500
})

const ErrorMessage = Object.freeze({
    INVALID_PASSWORD: "INVALID_PASSWORD"
})
module.exports = {ErrorCodes,ErrorMessage};